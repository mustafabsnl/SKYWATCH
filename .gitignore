# ══════════════════════════════════════════════════════
# SKYWATCH .gitignore
# GitHub'a sadece KOD gider, veri/model GITMEZ
# ══════════════════════════════════════════════════════

# ── ultralytics_skywatch: Kaggle'da kaggle_setup.py île patch ediliyor ──
# Bu klasöre artık gerek yok — tamamen dışarda bırak
ultralytics_skywatch/

# ── Veri Setleri (GB boyutunda) ─────────────────────
VeriSeti/
dataset_yolo/
data/
datasets/
*.zip
*.tar.gz
*.tar
*.7z

# ── Yerel Archive Dataset (fareselmenshawii/face-detection-dataset)
# Kaggle'da kagglehub ile indirilir — GitHub'a GITMEZ
archive/

# analyze_dataset.py'nin ürettiği data.yaml (lokal path içerir)
# Kaggle'da train_skywatch_kaggle.py tarafından dinamik oluşturulur
archive/data.yaml

# ── Model Ağırlıkları ────────────────────────────────
/models/                   # Sadece kök /models/ klasörü (trained weights)
# NOT: src/ultralytics_patch/cfg/models/ → KOD, gitignore'dan muaf
*.pt
*.pth
*.onnx
*.engine
*.tflite
*.torchscript
checkpoints/

# ── Ultralytics Eğitim Çıktıları ────────────────────
runs/
wandb/

# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/
pip-log.txt

# ── Sanal Ortam ──────────────────────────────────────
venv/
env/
.env
.venv/
ENV/

# ── Jupyter / Kaggle Notebook ────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Veritabanı ve Kişisel Veriler ───────────────────
database/skywatch.db
database/photos/
logs/
*.log

# ── Ekran Görüntüleri / Video / Büyük Binary ────────────────
logs/detections/
*.webm
*.mp4
*.avi
*.mov
# Rapor belgesi (büyük binary, git'te taşımak yoğuntur)
SKYWATCH_RAPOR.docx

# ── IDE ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# ── OS ──────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ── Geçici Dosyalar ──────────────────────────────────
*.tmp
*.bak
*.orig
*.rej
.cache/
tmp/

# ── ultralytics_skywatch — Derleme Artıkları ────────────────
# (Klasörün tamamı zaten ↗’de dışlandı)
ultralytics_skywatch/**/__pycache__/
ultralytics_skywatch/**/*.pyc
ultralytics_skywatch/**/*.egg-info/
ultralytics_skywatch/build/
ultralytics_skywatch/dist/

# ── Yerel Eğitim Çıktıları ────────────────────────────────
src/model/runs/

# Custom modeller ve özel dosyalar GitHub'a GİDER ✅
# src/ultralytics_patch/  ← GIT'TE KALIR
# src/model/              ← GIT'TE KALIR
# kaggle_setup.py         ← GIT'TE KALIR
